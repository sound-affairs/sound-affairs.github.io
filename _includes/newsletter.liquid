<!DOCTYPE html>
<html>
<head>
  <style>
    .newsletter-section {
      margin: 4rem auto 0;
      padding: 2.5rem 1rem;
      background-color: #f5f5f5;
      display: flex;
      justify-content: center;
      align-items: center;
    }
    
    .newsletter-container {
      max-width: 500px;
      width: 100%;
      text-align: center;
    }
    
    .newsletter-section p {
      margin-bottom: 1.5rem;
      color: #666;
      font-size: 1.1rem;
      line-height: 1.5;
    }
    
    .newsletter-section form {
      display: flex;
      gap: 0.75rem;
      width: 100%;
      max-width: 450px;
      margin: 0 auto;
    }
    
    .newsletter-section input {
      flex: 1;
      padding: 0.75rem 1rem;
      border: 2px solid #ddd;
      border-radius: 6px;
      font-size: 1rem;
      transition: border-color 0.2s;
    }
    
    .newsletter-section input:focus {
      outline: none;
      border-color: #666;
    }
    
    .newsletter-section button {
      padding: 0.75rem 1.5rem;
      background: #2563eb;
      color: white;
      border: none;
      border-radius: 6px;
      cursor: pointer;
      font-size: 1rem;
      font-weight: 500;
      transition: all 0.2s ease;
    }
    
    .newsletter-section button:hover {
      background: #1d4ed8;
      transform: translateY(-1px);
      box-shadow: 0 2px 4px rgba(37, 99, 235, 0.1);
    }
    
    .newsletter-section button:active {
      transform: translateY(0);
    }
    
    @media (max-width: 600px) {
      .newsletter-section {
        padding: 2rem 1rem;
      }
      
      .newsletter-section form {
        flex-direction: column;
      }
      
      .newsletter-section button {
        width: 100%;
      }
    }
  </style>
</head>
<body>
  <div class="newsletter-section">
    <div class="newsletter-container">
      <p>Subscribe to our newsletter to receive updates about our events and activities.</p>
      <form id="newsletterForm" onsubmit="handleSubmit(event)">
        <input type="email" id="emailInput" placeholder="Enter your email" required>
        <button type="submit">Subscribe →</button>
      </form>
    </div>
  </div>

  <script>
    function handleSubmit(event) {
      event.preventDefault();
      const email = document.getElementById('emailInput').value;
      const subject = encodeURIComponent("Please add me to newsletter");
      const body = encodeURIComponent(email);
      window.location.href = `mailto:soundaffairs-fgw@uva.nl?subject=${subject}&body=${body}`;
      document.getElementById('emailInput').value = '';
    }
  </script>
</body>
</html>